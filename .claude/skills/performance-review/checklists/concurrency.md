# Concurrency & contention checklist

**Purpose.** Surface lock-contention bottlenecks, atomic-ordering overhead, and parallelism that costs more than it saves.

**Triggers.** Code uses `Arc`, `Mutex`, `RwLock`, atomics, channels (`mpsc`, `crossbeam-channel`), `rayon::par_iter` / `rayon::scope`, `std::thread::spawn`, or a call into BLAS from a thread. **Also trigger when two thread pools can be live at once** — e.g. a `rayon` global pool *and* a `std::thread`/`crossbeam` worker set, nested `par_iter`, or `rayon` alongside a `tokio` runtime — even if no lock is in sight; oversubscription is a scheduler cost, not a lock cost.

**popnei.** `docs/architecture.md` section 3 fixes the topology: rayon over the records or over the rows of a block, the BLAS pool for the matrix products, never nested; a rayon worker that calls BLAS has it pinned to one thread; one read ahead thread between the reader and the consumer; and no threads at all under wasm. A BLAS pool (Accelerate, OpenBLAS) is a second thread pool in the sense of every rule below, and so is faer's own rayon use. The measurements of `docs/rust_core.md` are the baseline for what the products cost.

**Skip when.** Single-threaded code with no shared state. If dispatched anyway, write `No findings.`

**Note on overlap with the correctness review.** Some patterns in this category (weakened atomic ordering, manual `unsafe impl Send/Sync`) are correctness defects whose performance signature is a side effect. File the finding here with a measurement plan, but explicitly route it to the code review (`.claude/skills/code-review`) for the correctness call.

## Rules

- **Lock acquisition does not belong inside a hot loop.** Acquire once, batch the work, drop the guard. A common shape of this bug is a comparator or filter closure that locks shared state on every call from inside a sort or scan; the fix moves the acquisition out of the per-call path. Filed for any `lock().unwrap()` (or equivalent) inside a tight inner loop.
- **Lock granularity follows access patterns, not aesthetics.** A single `Mutex<HashMap<K, V>>` accessed concurrently is a serial bottleneck. Replacements, in increasing order of complexity:
  - `RwLock<HashMap<K, V>>` if reads dominate writes ≫ 10:1 (writer starvation is real; read-heavy is the precondition).
  - `dashmap::DashMap` for general per-bucket sharded access (sharded `RwLock`s; reduces but does not eliminate contention; writes still block readers in the same shard).
  - Per-shard `Vec<Mutex<HashMap<K, V>>>` where shard count and hash function are tuned to the workload. Highest manual cost, fewest surprises.
- **`std::sync::Mutex` vs `parking_lot::Mutex` is a contention-pattern decision, not a default-faster decision.** The "parking_lot is ~5× faster" maxim predates Rust 1.62, which moved `std::sync::Mutex` to futexes on Linux and SRW locks on Windows. On modern stdlib targets, std now wins or breaks even on short-hold workloads (Tokio's [#4623](https://github.com/tokio-rs/tokio/issues/4623) was opened explicitly because "futex … beats parking_lot in most cases"). The fairness argument still holds where it always did:
  - Short critical sections, low contention: `std::sync::Mutex` is the right default — cross-platform consistent, no extra dependency, recognised by gdb/perf/sanitizers.
  - Sustained heavy contention with one thread monopolising the lock: std's barging policy can starve other threads to near-zero progress; `parking_lot::Mutex`'s ~0.5 ms eventual-fairness handoff bounds worst-case wait and often *raises* total throughput by unblocking starved workers.
  - Bursty workloads, P99 latency targets, real-time-ish loops: `parking_lot` for the latency floor.
  - Platform skew is real: parking_lot still wins on some ARM Linux workloads; std can win on Windows under contention. Benchmark on the target hardware.
  - **Reviewer red flag:** a PR that adds `parking_lot` citing "it's faster" with no measurement is pre-2022 conventional wisdom. File the experiment, not the swap.
- **`parking_lot`'s durable advantages live in the API, not the speedometer.** These do not decay as std improves and are the legitimate reasons to take on the extra dependency:
  - **Mapped guards** (`MappedMutexGuard`, `MappedRwLockReadGuard`, `MappedRwLockWriteGuard`) project a guard onto a sub-field of the locked value. No std equivalent — workaround is a hand-rolled wrapper that re-locks.
  - **`RwLock` reader/writer bias is configurable** (reader-biased, writer-biased, or fair). std's bias is implementation-defined and not selectable.
  - **Atomic `RwLock` downgrade** (write → read with no release window in between).
  - **Mutex footprint** is 1 byte vs std's ~5–40 bytes. Only matters when many fine-grained locks dominate the struct after alignment — `Mutex<u64>` saves nothing.
  - **`Condvar` is spurious-wakeup-free.**
- **`parking_lot`'s no-poisoning is a trade-off, not a free win.** `std::sync::Mutex` poisons on panic-in-critical-section, forcing the next locker to acknowledge the half-updated state. `parking_lot` simply unlocks. Convenient, but a panic mid-mutation silently exposes broken invariants to the next caller. If a swap to parking_lot is justified for other reasons, the code must be panic-safe inside its critical sections — or the loss of poisoning must be a stated, accepted decision.
- **Don't expose `parking_lot` types in a library's public API.** `parking_lot::MutexGuard` differs from `std::sync::MutexGuard` in its `Send` / `UnwindSafe` impls; once a guard type leaks across a crate boundary, swapping lock crates becomes a breaking change. Keep parking_lot internal — exposed APIs should return `&T` / `&mut T` or take a closure, not raw guards. (Tokio's reluctance to migrate off `parking_lot` despite the perf reversal is rooted in exactly this concern.)
- **`RwLock` is not a free upgrade from `Mutex`.** It has more overhead in the uncontended case; `std::sync::RwLock` is also vulnerable to writer starvation (`parking_lot::RwLock` is fair by default but does not change the uncontended-overhead trade). Justified only when reads vastly outnumber writes and a measurement shows `Mutex` blocks readers.
- **Memory orderings are justified by the synchronization relationship.** Default to `SeqCst` and only weaken with a comment naming what loads pair with what stores. `Relaxed` is correct for independent counters and free-running statistics; `Relaxed` for synchronization without a separate fence is a Hot-path correctness finding (route through correctness review).
- **Channels: bounded by default.** Unbounded channels (`mpsc::unbounded_channel`, `crossbeam_channel::unbounded`) silently buffer until the process OOMs. Filed for any unbounded channel reading from an external or large source.
- **`rayon::par_iter` over short work is slower than the serial version.** Fork-join overhead bites when per-element work is below a few hundred nanoseconds. `par_iter()` over collections of small numeric ops without a chunking strategy or `with_min_len` tuning is a finding. Run the serial-vs-parallel benchmark before merging.
- **Rayon's default splitting assumes roughly uniform per-item cost.** Pre-divided binary-tree ranges leave fast threads idle while one thread finishes the heavy tail when per-item cost varies widely (e.g., biallelic variants beside highly multiallelic ones, populations of very different sizes). `with_min_len` controls split *depth*, not runtime balance, so it does not fix this. Symptoms: high wall-clock variance across runs, threads finishing at very different times, `perf` showing futex/sched_yield concentrated near end-of-run. Mitigations to consider: cost-aware partitioning at the producer (split heavy items before handing to Rayon), smaller chunks via `with_min_len`, or a custom adaptive stealing loop. Profile with `perf` before tuning — Rayon overhead is often not the real bottleneck.
- **Atomic counters cause cross-CPU traffic.** A single global `AtomicU64` incremented from many threads becomes the bottleneck on its own cache line. Patterns: per-thread counters summed at end-of-phase, or `crossbeam_utils::CachePadded<AtomicU64>` per shard. Cross-reference: `data_layout.md` on false sharing.
- **`Arc::clone` is one atomic increment, not free.** Gratuitous `Arc::clone` in a hot loop (e.g., capturing into a closure that does not outlive the iteration) shows up. Borrow when possible.
- **Two thread pools sized to the core count oversubscribe by 2×.** When a `rayon` global pool *and* a separately-spawned worker set (`std::thread::scope`, `crossbeam`, a second `rayon::ThreadPool`, or a `tokio` runtime) are both live and both sized to `available_parallelism()` / `--threads`, the machine runs `~2·N` CPU-bound threads on `N` cores. The default of every pool is "I own the machine" — two such defaults collide. This is a scheduler cost (context switches, futex waits), not a lock cost, and it is invisible to lock-focused rules. **See the diagnostic playbook below.** Filed whenever a producer/consumer pipeline pairs a parallel producer (rayon fold/decode) with a parallel consumer (worker threads).
- **Nested `par_iter` (or a blocking call inside `par_iter`) can starve or deadlock the pool, not just slow it.** A `par_iter` whose closure itself calls `par_iter`, or blocks on a full channel / `Mutex` / I/O, occupies a pool thread while making no progress; enough of them exhaust the fixed pool. Symptom is a *stall*, not just high wall — confirm with a debugger stack dump (below), and isolate the inner/ blocking work onto its own `ThreadPool::install(...)` or off the rayon pool entirely. The deadlock side of it goes to the code review.

## Diagnosing thread oversubscription and pool collisions

A distinct sub-task: when a parallel program is *slower than expected as thread count rises* (or scales worse than `main` / a prior version specifically at T≥2), the cause is often **not** any single hot function but **too many runnable threads fighting for too few cores**. The classic shape is two thread pools, each defaulting to "use all cores," live at the same time. This playbook is for confirming or refuting that before recommending a topology change — do not pattern-match a pool rewrite without the evidence below.

### The shapes that cause it

- **Coexisting pools.** A `rayon` global pool (driving a `par_iter`/`par_iter_mut` producer fold or decode) running concurrently with a separate worker set spawned by `std::thread::scope` / `crossbeam` / a second `rayon::ThreadPool` / a `tokio` runtime — both sized to the same `N`. Net `~2·N` CPU-bound threads on `N` cores.
- **Nested parallelism.** `par_iter` inside `par_iter` (the inner one re-enters the same global pool). E.g. *"30 fields each wanting 16 sort threads on a 16-thread pool = 30× theoretical oversubscription."*
- **A wrong core count.** Sizing to the host core count (or an old toolchain's `available_parallelism()`) **overcounts** the parallelism the process actually owns under a cgroup quota/affinity mask; both Tokio and Rayon then build pools to a number the scheduler will throttle. (PostHog: pinning to the CPU *request* instead of the *limit* was part of the fix.) Verified specifics:
  - `std::thread::available_parallelism()` became cgroup-aware only **gradually** — cgroups **v2 in Rust 1.61**, **v1 in 1.64** (rust-lang [#92697](https://github.com/rust-lang/rust/pull/92697); rayon [#937](https://github.com/rayon-rs/rayon/pull/937)). The std docs explicitly warn it "may overcount … when limited by a process-wide affinity mask or cgroup quotas" and "when running in a VM with CPU usage limits." On an older toolchain, or when reading raw core count, the container number is wrong.
  - **Size from the quota, not the machine:** effective CPUs = `quota / period` (cgroup v2 `cpu.max`; v1 `cpu.cfs_quota_us` / `cpu.cfs_period_us`). `quota == period` → 1 CPU; `quota == 2·period` → 2 CPUs. `num_cpus` derives this too but uses **ceiling** division (a 1.5-CPU quota rounds up to 2 → a mild overcount of its own).
- **Blocking inside a pool thread.** Channel send/recv, `Mutex`, or I/O inside a `par_iter` closure parks a worker while holding its slot (see the nested-`par_iter` rule) — this can *stall*, not just slow.
- **`par_bridge` on an underfed serial iterator.** This is a *single-pool* failure mode that mimics oversubscription: `par_bridge()` pulls items from the serial iterator one at a time under a shared lock, and when the producer (a channel, an I/O-bound reader) cannot keep up, the rayon workers **busy-wait/spin** for the next item instead of parking — driving every core to ~100 % CPU with *negative* thread scaling (worse wall than serial or a single-pool baseline). Documented in rayon [#730](https://github.com/rayon-rs/rayon/issues/730) (185 s with `par_bridge` vs 135 s without, on a bounded channel) and [#795](https://github.com/rayon-rs/rayon/issues/795) (throughput peaks at 8–16 threads on a 64-core box then degrades while "all the CPUs are churning at 100 %"). The `ParallelBridge` docs themselves warn it "can be less efficient than if you started with `par_iter`" and that the serial iterator "may become a bottleneck if it can't keep up." Fixes are `par_bridge`-specific, *not* the pool-sizing recipe: **batch** the feed (pull/collect N items into a `Vec`, then `par_iter` the batch — restored scaling in #795), or drop `par_bridge` for an explicit bounded-channel fan-out to worker threads. Distinguish from two-pool oversubscription by checking the source: one pool + `par_bridge` over a slow iterator → this; two pools each sized to `N` → the rule above.

### Symptoms in a sampling profile (what to look for)

The signature is **threads blocked waiting, plus scheduler churn, while mean CPU utilisation stays low and per-item work is unchanged.** Concretely:

- **The producer/dispatcher thread spends most of its samples *blocked* inside the pool's wait primitive**, not doing work. For rayon that is `bridge_producer_consumer` → `in_worker_cold` → `LockLatch::wait_and_reset` → the OS wait. If the thread that *should* be feeding work is 50–70 % parked in the pool latch, it is waiting on workers that the scheduler cannot run.
  - **Do not misread `bridge_producer_consumer` as `par_bridge`.** `bridge_producer_consumer` is rayon's generic driver for **indexed** parallel iterators — `par_iter()` / `par_iter_mut()` over slices and ranges split through it. `par_bridge()` (the adapter that parallelises an arbitrary serial `Iterator`) uses a *different* path (`IterBridge` / `bridge_unindexed`). Seeing `bridge_producer_consumer` in the stack means a slice/range `par_iter[_mut]` is blocking the calling thread on its `install`/`join` latch while the workers run — the textbook two-pool-oversubscription frame. It does **not** implicate `par_bridge`, and the `par_bridge`-specific remedies below (batch the feed, replace the bridge) do not apply. Confirm which adapter is actually in the source before prescribing.
- **Scheduler / futex frames rise with thread count.** Compare a T=1 capture against a high-T capture of the *same* workload:
  - **Linux (`perf`)**: `futex`, `__sched_yield`, high **involuntary** context-switch count (`perf stat -e context-switches,cpu-migrations`; `perf sched`), `finish_task_switch`.
  - **macOS (`sample`)**: `__psynch_cvwait`, `__psynch_mutexwait`, `__ulock_wait`/`__ulock_wait2`, `swtch_pri`, `semaphore_wait_trap`. These growing *as a share* from T=1 to T=8 (rather than staying flat) is the tell. (Note: a parked rayon pool also shows idle `Sleep::sleep` frames — those are normal idle, distinct from the *contention* frames above.)
- **Thread count ≫ core count.** Count the OS threads in the profile's thread list and compare to physical cores. `~2·N` threads with `N` cores is the smoking gun; the profiler's per-thread roots make this a 10-second check.
- **Low mean CPU, spiky demand.** PostHog's breakthrough metric was **CFS throttling spiking in lockstep with p99 latency while mean CPU utilisation stayed low** — brief intense bursts of more-threads-than-cores, not sustained saturation. The mechanism: CFS bandwidth control limits *amortized* usage — a task group gets `quota` µs per `period`; a parallel burst can spend the whole quota in a fraction of the period, after which **every thread is throttled (slept) until the next period replenishes it** (kernel [sched-bwc](https://docs.kernel.org/scheduler/sched-bwc.html); Dan Luu, ["disastrous for tail latency"](https://danluu.com/cgroup-throttling/)). Oversubscription compounds this — more runnable threads burn the quota faster. Detect via cgroup `cpu.stat`: `nr_throttled` (count of throttle events) and `throttled_time` (ns; **cgroup v2 renames these to `nr_throttled` count + `throttled_usec`**). Rising `nr_throttled` relative to `nr_periods`, with `throttled_time`/`throttled_usec` accumulating, means CFS throttling is the bottleneck (or compounding the oversubscription) — treat **>30 % throttling** as "reduce oversubscription." This is a *distinct* failure mode from raw oversubscription: it can bite even at the "right" thread count if the quota is small.

### Confirm it before prescribing

- **Diff two thread counts.** Re-run the workload at T=1 and at high T and compare the contention-frame share. If the gap appears *only* at T≥2 and grows with T, oversubscription is implicated (per-item cost is constant, so a per-function fix would not be T-dependent).
- **Count threads vs cores** from the profile's thread roots.
- **For a stall (not a slowdown), dump stacks.** Load the stuck process in a debugger (`lldb`/`gdb`, or `sample`/`eu-stack` once) and read every thread's call stack — exhaustion shows as "stage A's threads all blocked on a full channel, stage B's threads all waiting for a free pool worker." A debugger snapshot is faster than reasoning about it.

### Fixes (cheapest first; each is its own experiment)

1. **Size the pools so the total ≈ cores, not `2·cores`.** Cap one pool below the core count (`ThreadPoolBuilder::num_threads`, or pass `N − k` to whatever sizes the rayon pool) so producer-pool + consumer-pool ≈ `N`. One-line change; sweep the split. *"Tokio gets half the cores for I/O, Rayon the full count, accepting mild oversubscription since the I/O threads are mostly parked"* is the asymmetric version — parked threads don't contend, CPU-bound ones do, so reserve cores for whichever side is actually on-CPU.
2. **Collapse to one pool.** Run both phases on the same pool (move the producer's parallel work onto the consumer threads, or vice-versa) so there is a single scheduler of `N` threads. Largest change; highest risk if it reorders work — gate on output-identity.
3. **Serialize the cheaper side.** If one pool's parallel section is small, drop its `par_iter` and let the other side own all the cores. Often the simplest win.
4. **Isolate, don't share.** For nested or blocking work, give it its own `rayon::ThreadPool` via `ThreadPool::install(...)` so it cannot starve the main pool; or move blocking I/O off the pool entirely (`spawn_blocking` for tokio).
5. **Sequential below a threshold.** For small inputs, skip parallelism entirely (PostHog evaluated batches under 100–200 items sequentially) — fork-join + oversubscription overhead dominates tiny work.
6. **Fix the core count.** In a container, derive the pool size from the CPU quota you actually own — `quota / period` from `cpu.max` (v2) or `cpu.cfs_quota_us`/`cpu.cfs_period_us` (v1) — not the host core count and not an old toolchain's `available_parallelism()`. Verify on the target with `cpu.stat` (`nr_throttled` / `throttled_time`) that throttling actually falls after the change; a number that *looks* right but still throttles means the split is still too high.

### Measurement plan template for an oversubscription finding

- **Metric:** wall time at T=1, 2, 4, 8 (and peak RSS if memory is a project constraint), best-of-3 median; **plus** the contention-frame share (`swtch_pri`/`__ulock_wait`/`futex`/throttling) from a fresh profile at the top T.
- **Threshold to merge:** the high-T wall gap closes by a meaningful fraction **and** the contention frames fall toward their T=1 level, with **output identical** (diff whatever the project's correctness oracle is — calls, bytes, results).
- **Complexity cost:** name it honestly — pool-sizing is trivial; collapsing/relocating a pool is a topology change that can reorder work and threaten output-identity, so it must be gated on the oracle and is usually deferred behind the one-line sizing experiment.

Sources: PostHog, [*Untangling Tokio and Rayon in production*](https://posthog.com/blog/untangling-rayon-and-tokio); Daniel Imfeld, [*Avoiding Deadlock from Rayon Thread Pool Exhaustion*](https://imfeld.dev/writing/avoiding_rayon_thread_pool_exhaustion); Piotr Kołaczkowski, [*Multiple Thread Pools in Rust*](https://pkolaczk.github.io/multiple-threadpools-rust/); rayon [`ThreadPool`](https://docs.rs/rayon/latest/rayon/struct.ThreadPool.html) / [`ThreadPoolBuilder`](https://docs.rs/rayon/latest/rayon/struct.ThreadPoolBuilder.html) / [`ParallelBridge`](https://docs.rs/rayon/latest/rayon/struct.ParallelBridge.html) docs; rayon issues [#730](https://github.com/rayon-rs/rayon/issues/730) / [#795](https://github.com/rayon-rs/rayon/issues/795) (par_bridge starvation) and [#937](https://github.com/rayon-rs/rayon/pull/937) (cgroup awareness); Rust std [`available_parallelism`](https://doc.rust-lang.org/stable/std/thread/fn.available_parallelism.html) docs + rust-lang [#92697](https://github.com/rust-lang/rust/pull/92697); Linux kernel [*CFS Bandwidth Control*](https://docs.kernel.org/scheduler/sched-bwc.html); [`num_cpus` cgroup parsing](https://github.com/seanmonstar/num_cpus/blob/master/src/linux.rs); Dan Luu, [*CPU bandwidth control / cgroup throttling*](https://danluu.com/cgroup-throttling/); Intel VTune [*Threading Efficiency View*](https://www.intel.com/content/www/us/en/docs/vtune-profiler/user-guide/2024-2/threading-efficiency-view.html). Full verified-claim research log: [doc/devel/reports/research/h1_thread_oversubscription_2026-06-07.md](../../../../doc/devel/reports/research/h1_thread_oversubscription_2026-06-07.md).
